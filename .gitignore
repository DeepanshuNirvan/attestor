node_modules/
dist/
.astro/
.next/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
.pnpm-store/
apps/website/public/pagefind/
infra/data/
tmp/

# Local editor and preview configuration.
.claude/launch.json

# Demo credentials written by `pnpm seed`.
.seed-credentials.txt

# Throwaway end-to-end harnesses. They drive the real API, so they hold a real password and a real
# TOTP secret for the local stack; `tmp-e2e-state.json` was one `git add -A` away from the history.
tmp-*.mjs
tmp-*.json
*-e2e.mjs
